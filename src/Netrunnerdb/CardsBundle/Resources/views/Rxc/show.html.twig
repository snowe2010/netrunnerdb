<h1>Rxc</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Idrulings</th>
            <td>{{ entity.idrulings }}</td>
        </tr>
        <tr>
            <th>Idcards</th>
            <td>{{ entity.idcards }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('admin_rxc') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_rxc_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('admin_rxc_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
